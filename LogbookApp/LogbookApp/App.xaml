<Application
    x:Class="LogbookApp.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LogbookApp">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!-- 
                    Styles that define common aspects of the platform look and feel
                    Required by Visual Studio project and item templates
                 -->
                <ResourceDictionary Source="Common/StandardStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="TotalsAppBarButtonStyle" TargetType="ButtonBase" BasedOn="{StaticResource CalculatorAppBarButtonStyle}">
                <Setter Property="AutomationProperties.AutomationId" Value="TotalAppBarButton"/>
                <Setter Property="AutomationProperties.Name" Value="Totals"/>

                <Setter Property="Foreground" Value="Cyan"></Setter>
            </Style>
        </ResourceDictionary>
     
    </Application.Resources>
</Application>
